body {
  font-family: sans-serif;
  text-align: center;
  margin-top: 50px;
}
input, button {
  margin: 10px;
  padding: 10px;
}
#ticketDisplay {
  margin-top: 20px;
  border: 1px dashed #333;
  padding: 20px;
  display: none;
}
